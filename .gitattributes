# This repository uses Unix line endings. Normalize on commit and check out LF
# everywhere, so a Windows checkout cannot reintroduce CRLF.
* text=auto eol=lf

# Binary-ish files git should never touch
*.png binary
*.jpg binary
*.pdf binary
*.ico binary
